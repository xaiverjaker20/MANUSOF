document.getElementById('loginForm').addEventListener('submit', function(event) {
    // No es necesario prevenir el comportamiento por defecto
    // ya que el formulario se enviará al servidor para validación
});